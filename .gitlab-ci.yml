image: alpine
before_script:
  - git clone https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.quintagroup.com/openprocurement.api.git  openprocurement.api
  - ./bootstrap.sh
  - bin/buildout -N
tests:
  script:
    - bin/py.test openprocurement/frameworkagreement/cfaua/tests --cov=openprocurement.frameworkagreement.cfaua
