image: alpine
before_script:
  - git clone https://gitlab-ci-token:${CI_JOB_TOKEN}@gitlab.quintagroup.com/op/openprocurement.api.git code1
  - rm -rf code1 code
  - ./bootstrap.sh
  - bin/buildout -N
tests:
  script:
    - bin/py.test openprocurement/frameworkagreement/cfaua/tests --cov=openprocurement.frameworkagreement.cfaua
